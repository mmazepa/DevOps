apiVersion: v1

kind: Service

metadata:
  name: myapp-service

spec:
  type: NodePort
  ports:
    - targetPort: 3000
      port: 80
      NodePort: 30009

  selector:
    app: myapp
    type: frontend
